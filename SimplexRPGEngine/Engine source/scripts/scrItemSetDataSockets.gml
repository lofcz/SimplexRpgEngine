/// scrItemSetDataSockets(value1, value2...)

for (var i = 0; i < 16; i++)
    {
     if (argument_count > i)
        {
         vlastnost[vlastnost_dataSocket1 + i] = argument[i];
        }
     else {break;}
    }
