/// scrUpdateAffects()

var index;

for (i = 0; i < ds_list_size(oPlayer.statusList) i++)
    {
     index = oPlayer.statusList[| i];
     
     // Poison
     if (index == "poison")
        {
         
        }
    
    }
